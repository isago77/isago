
/**
 * API 입력값에 대한 최대 길이 제한을 정의하는 열거형입니다.
 * 
 * 각 필드는 프론트엔드 또는 백엔드에서 입력값의 유효성을 검사할 때 사용되며,
 * 실제 저장소(DB)나 사용자 입력 폼에서도 동일한 기준을 적용할 수 있도록 상수로 선언합니다.
 */
export enum APILength {
    url = 512,
    email = 320,
    uuid = 36,
    token = 64,
}